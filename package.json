{
  "name": "tasklist-railway",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix tasklist",
    "railway:build": "npm install --prefix server && npm install --prefix tasklist && npm run build --prefix tasklist",
    "start": "npm start --prefix server",
    "import:tasks": "npm run import:tasks --prefix server"
  },
  "engines": {
    "node": ">=22"
  }
}
